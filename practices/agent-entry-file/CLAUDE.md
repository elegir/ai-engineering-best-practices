@AGENTS.md

## Claude Code
- Start non-trivial work in plan mode; present the plan; wait for approval.
- Use `<<top-tier model>>` for planning/spec tasks and `<<mid-tier model>>` for atomized implementation (see `docs/workflow.md` §3).
- Hooks are configured in `.claude/settings.json`; do not modify them.
- Personal notes go in `CLAUDE.local.md` (gitignored), never here.
